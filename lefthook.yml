pre-commit:
  parallel: true
  commands:
    lint:
      run: make lint
    fmt:
      run: make fmt

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  commands:
    test:
      run: make test
